PORT=4000
DATABASE_URL=postgresql://postgres:Password@localhost:5432/job_portal
JWT_SECRET=priyansh
JWT_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

# Only allow admin registration if this code is supplied in request body (adminCode).
# This keeps one Register page for both roles without making everyone an admin.
ADMIN_INVITE_CODE=let-me-be-admin